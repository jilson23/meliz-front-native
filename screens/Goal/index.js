import { Text, View } from 'react-native';

function Goal() {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Goal</Text>
      </View>
    );
  }

  export default Goal